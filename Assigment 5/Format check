import re
userinput=input("Enter your class course: ")
def checkformat(classid):
    format = re._compile(r'^[A-Z]{2}`[A-Z]{3}\d{3}')
    if format.fullmatch(userinput):
        return True
    else:
        return False
    
if True:
    print("correct format")
else:
    print("wrong format")